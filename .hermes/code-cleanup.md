You are a senior engineer doing code cleanup.

Working directory: D:\Personal projects\DataSmith

Find and remove dead, unused, duplicate, and unnecessary code without changing behavior. The project has 18 Python source files across datasmith/ (core, schema, generation, imperfections, llm, ui packages) + 2 Streamlit pages.

Focus on:
- unused variables, functions, imports, components, and files
- duplicate logic and repeated helpers
- obsolete branches and feature flags
- unnecessary abstractions and wrappers
- commented-out code and stale TODOs
- redundant dependencies (check pyproject.toml + requirements.txt)
- dead test code and orphaned test fixtures

Rules:
- preserve existing functionality
- prefer deletion over refactoring unless refactor is clearly needed
- keep changes minimal and safe
- explain why each removal is safe
- NEVER add new features or change behavior

Method:
1. Run full test suite first: cd "D:/Personal projects/DataSmith" && ".venv/Scripts/python.exe" -m pytest tests/ --tb=short
2. Read ALL Python source files (datasmith/**/*.py + pages/*.py + scripts/*.py + app.py)
3. Find: unused imports (run `vulture` or manual scan), dead variables, functions never called, duplicate code blocks, stale comments/TODOs
4. Remove them with patch tool (surgical, one removal per patch where possible)
5. Run full test suite after every 2-3 removals to catch breakage
6. Report final diffstat and test count

Quick wins to check first:
- datasmith/imperfections/__init__.py — is there anything useful or just re-exports?
- datasmith/__init__.py — same
- scripts/analyze_domains.py — is this used or dead?
- scripts/crawl_schemas.py — vs crawl_and_analyze.py, which is the active one?
- docs/PERFORMANCE.md generated by OpenCode — is it stale or useful?
- tests/TESTING_STRATEGY.md generated by OpenCode — same
- Any duplicate utility functions across files
- Any commented-out code blocks
- Any imports of modules that were removed in earlier refactors
- pyproject.toml: any deps listed that nothing imports

Output:
1. What was deleted and why each removal is safe
2. Final diffstat and test count
3. Any risks or follow-up work recommended

Do NOT commit. Just apply patches and report.
